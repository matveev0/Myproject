package netcracker.comparator;

import netcracker.entity.Person;

import java.util.Comparator;

/*public interface PersonComparator {
    Comparator<Person> COMPARE_BY_ID = new Comparator<Person>() {
        @Override
        public int compare(Person lhs, Person rhs) {
            return lhs.getId().compareTo(rhs.getId());
        }
    };
    Comparator<Person> COMPARE_BY_SURNAME = new Comparator<Person>() {
        @Override
        public int compare(Person lhs, Person rhs) {
            return lhs.getFIO().compareTo(rhs.getFIO());
        }
    };
    Comparator<Person> COMPARE_BY_AGE = new Comparator<Person>() {
        @Override
        public int compare(Person lhs, Person rhs) {
            return lhs.getAge() - rhs.getAge();
        }
    };
}*/
