package netcracker.comparator;

/*public interface PersonComparator {
    Comparator<IPerson> COMPARE_BY_ID = new Comparator<IPerson>() {
        @Override
        public int compare(IPerson lhs, IPerson rhs) {
            return lhs.getId().compareTo(rhs.getId());
        }
    };
    Comparator<IPerson> COMPARE_BY_SURNAME = new Comparator<IPerson>() {
        @Override
        public int compare(IPerson lhs, IPerson rhs) {
            return lhs.getFIO().compareTo(rhs.getFIO());
        }
    };
    Comparator<IPerson> COMPARE_BY_AGE = new Comparator<IPerson>() {
        @Override
        public int compare(IPerson lhs, IPerson rhs) {
            return lhs.getAge() - rhs.getAge();
        }
    };
}*/
